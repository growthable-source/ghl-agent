'use client'

/**
 * Voice agent — When-to-run tab. Re-exports the shared triggers
 * page (the agent-side trigger config: channels + CRM events that
 * route inbounds to this agent).
 */

export { default } from '../../../agents/[agentId]/trigger/page'
