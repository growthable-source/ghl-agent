'use client'

/**
 * Voice agent — Knowledge tab. Re-exports the shared agent knowledge
 * editor; same content under the voice URL space.
 */

export { default } from '../../../agents/[agentId]/knowledge/page'
