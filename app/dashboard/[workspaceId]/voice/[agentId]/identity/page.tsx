'use client'

/**
 * Voice agent — Identity tab. Re-exports the shared identity overview
 * (system prompt, instructions, fallback behaviour, calendar settings).
 */

export { default } from '../../../agents/[agentId]/identity/page'
